import classNames from 'classnames';
import './App.scss';

const appClasses = {
    appStart: classNames(
        'startTheme',
    ),
    appBg: classNames(
        'mainTheme',
    ),
    appInn: classNames(
        'innerBox',
    ),
    alertMsg: classNames(
        'alertMsg',
    ),
}

export default appClasses;